# File: backend/.env.example
# Version: v0.1.0
# Example environment for CornStructor backend + Alembic

# PostgreSQL (recommended)
# DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/cornstructor

# SQLite (dev fallback)
DATABASE_URL=sqlite:///./cornstructor.db
